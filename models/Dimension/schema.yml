version: 2

models:
  - name: dim_customers
    description: "Dimension client (SK générée)"
    columns:
      - name: customer_id
        tests:
          - not_null
  - name: dim_prouct
    description: "Dimension Produit"
    columns:
      - name: product_id
        tests:
          - not_null