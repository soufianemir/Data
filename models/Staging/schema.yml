version: 2

models:
  - name: stg_customers
    description: "Staging layer pour customers"
    columns:
      - name: customer_id
        tests:
          - not_null
  - name: stg_products
    description: "Dimension Produit"
    columns:
      - name: product_id
        tests:
          - not_null
